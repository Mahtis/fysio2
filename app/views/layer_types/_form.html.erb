<%= form_with(model: layer_type, local: true) do |form| %>
  <% if layer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(layer_type.errors.count, "error") %> prohibited this layer_type from being saved:</h2>

      <ul>
      <% layer_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :layer_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
